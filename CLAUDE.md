# Claude Code Instructions

This project uses `AGENTS.md` as the canonical agent context file. See [AGENTS.md](./AGENTS.md).

Project-specific Claude memory (auto-generated session notes) lives in `.claude/memory/` and is gitignored.

Personal preferences not committed to git live in `CLAUDE.local.md` (also gitignored).

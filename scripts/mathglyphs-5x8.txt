# Hand-drawn 5x8 math glyphs for the Spleen small font (the BDF has no
# Greek coverage at this size). Consumed by bdf_to_utft.py --extra.
# '#' = pixel on, '.' = off. 5 columns wide, 8 rows tall; styled to
# match Spleen 5x8's 1px strokes (x-height rows 2..6, col 4 spacing —
# full-width caps like 'T' use all 5 columns, as the pi bar does here).
# Slot 127 mirrors kGlyphPi in the 8x16 fonts (graph pi-tick labels).

# U+03C0 pi: full-width top bar over two legs.
@ 127 pi
.....
.....
#####
.#.#.
.#.#.
.#.#.
.#.#.
.....

# Serial line injection — superseded

This scoping document was promoted to a phase spec on 2026-08-08.

**See [`docs/phases/phase5.1-spec.md`](../phases/phase5.1-spec.md)** — it
carries the full design (the `submit_line()` extraction argument, the wire
protocol, the `ResultKind` finding, the recorded limits) plus a task breakdown
and open questions, in the standard phase-spec shape.

Kept as a stub rather than deleted because commit `dd270c1` and the
2026-08-08 worklog entry reference this path.
